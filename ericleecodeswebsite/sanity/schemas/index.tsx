import project from "./project-schema";
import page from "./page-schema";
import work from "./work-schema";

const schemas = [project, page, work];
// const schemas = [project];

export default schemas;
